server:
  port: 8084

spring:
  application:
    name: Customer-Service

  cassandra:
    contact-points: customer_cassandra
    port: 9042
    keyspace-name: customer_keyspace
    local-datacenter: datacenter1
    schema-action: create_if_not_exists
